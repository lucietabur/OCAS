{% extends "@OCAS/layout.html.twig" %}
{# changer l'heritage de template #}
{% block title %}{{h1}}{% endblock %}

{% block header %}{{h1}}{% endblock %}
{% block content %}

<div class="col-8 offset-2">
  {% form_theme form "bootstrap_4_horizontal_layout.html.twig" %}
  {{ form_start(form)}}
  {{ form_widget(form)}}
  {{form_end(form)}}
  <a role="button" href="{{ path('intervenant_list')}}" class="btn btn-primary">Retour à la liste</a>
  {% if id is defined %}
    <a role="submit" href="{{ path('intervenant_delete',{'id': intervenant.id })}}" class="btn btn-danger">Supprimer</a>
  {% endif %}
  </div>
{% endblock %}
