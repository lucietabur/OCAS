<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>OCAS : {% block title %}{% endblock %}</title>
        {% block stylesheets %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        {#}<link rel="stylesheet" href="/static/css/main.css">#}
        <link rel="stylesheet" href="{{ asset('/static/css/bootstrap.min.css') }}">
        {% endblock %}
    </head>
    <body>
      <header class="index-header">
        <h1>{% block header %}ORDRE DE MISSION{% endblock %}</h1>
      </header>
      <div>
        {% block address %}
        <p>{{ stagiaire.nom }}</p>
        <p>{{ siege.correspondant}}</p>
        <p>{{ siege.nom }}</p>
        <p>{{ siege.adresse }}</p>
        <p>{{ siege.adresse_complement }}</p>
        <p>{{ siege.code_departement }} {{ siege.ville }}</p>
        {% endblock %}
      </div>
      <div class='col-2'>
        {% block expediteur %}
          <p>Rectorat</p>
          <p>GIP-FCIP de l'académie de Caen</p>
          <p>Réf : {{ref}}</p>
          <p>Affaire suivie par : {{suivi}}</p>
          <p>Télephone: {{correspondant.telephone}} 0231301580</p> {# a enregistrer quelque part #}
          <p>Télécopie : {{correspondant.telecopie}} 0231867928</p>
          <p>Mel : {{correspondant.mel}}</p>
          <p>Rectorat de Caen</p>
          {{correspondant.adresse}}
          {{correspondant.adresse_complement}}
          {{ correspondant.code_departement }} {{ correspondant.ville }}
        {% endblock %}
      </div>
      <div class="col-10">

      {% block content %}
        Fonction : {{ stagiaire.fonction }}
        Résidence administrative : {{ agence.nom }}
        Se rendra au : {{agence.nom }}
        En qualité de participant•e, le(s) : {{ formation.date_debut }}
        de : {{ formation.heure_debut }} à {{ formation.heure_fin }}
        Imputation budgétaire: {{imputation}}
        <form>
          <legend>Moyen de transport utilisé :</legend>
          <input type="checkbox" name="transport" value="transport"> transport en commun (joindre le titre de transport)</input>
          <input type="checkbox" name="vehicule" value="vehicule"> véhicule personnel sur autorisation du Directeur du GIP-FCIP
        </form>


      {% endblock %}
    </div>
      {% block javascripts %}{% endblock %}
    </body>
</html>
