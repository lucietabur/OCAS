{% extends 'base.html.twig' %}
{% block title %} OCAS {% endblock %}
{% block header %}Outils Complémentaires A Stage{% endblock %}
{% block content %}
  <div class="h-75 d-flex justify-content-around">
      <div class="col-3">
          {% for nom,lien in menu.getMenu()['liste'] %}
            <a role="button" class="mybutton d-flex justify-content-center align-items-center index btn btn-primary" href="{{ path(lien)}}">{{nom}}</a>
          {% endfor %}
      </div>
      <div class="col-3">
          {% for nom,lien in menu.getMenu()['frequent'] %}
            <a role="button" class="mybutton d-flex justify-content-center align-items-center index btn btn-primary" href="{{ path(lien)}}">{{nom}}</a>
          {% endfor %}
      </div>
  </div>
  <div class="h-25 row justify-content-around">
          {% for nom,lien in menu.getMenu()['rare'] %}
            <a role="button" class="mybutton d-flex justify-content-center align-items-center index btn btn-primary col-2" href="{{ path(lien)}}">{{nom}}</a>
          {% endfor %}
  </div>
{% endblock %}
